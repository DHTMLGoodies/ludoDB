{
	"table": "grand_parent",
	"sql": "select * from grand_parent where id=?",
	"columns": {
		"id": "int auto_increment not null primary key",
		"zip": {
			"db": "varchar(25)",
			"references": "a_city(zip) on delete cascade"
		}
	}
}